local M = {}

--- @param tests Test[]
local function convertTestsToTestsTree(tests)
    local workingTestsCollection = {}
    for i, x in pairs(tests) do

        workingTestsCollection[i] = x
    end

end
